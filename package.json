{
  "name": "react-adminlte-clean-arch",
  "private": true,
  "scripts": {
    "dev:client": "npm --prefix admin run dev:client",
    "dev:server": "npm --prefix backend run start:dev",
    "dev:all": "npm --prefix admin run dev:all",
    "build:client": "npm --prefix admin run build",
    "build:server": "npm --prefix backend run build",
    "preview": "npm --prefix admin run preview",
    "server": "npm --prefix backend run start",
    "install:all": "npm --prefix admin install && npm --prefix backend install"
  },
  "workspaces": [
    "admin",
    "backend"
  ]
}
